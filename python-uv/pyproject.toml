[project]
name = "bootstrapper"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
bootstrapper = "bootstrapper:main"

[build-system]
requires = ["uv_build>=0.9,<0.10"]
build-backend = "uv_build"
